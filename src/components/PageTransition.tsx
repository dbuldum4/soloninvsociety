'use client';

// Transition disabled; simple pass-through component retained for potential future use
export default function PageTransition({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
